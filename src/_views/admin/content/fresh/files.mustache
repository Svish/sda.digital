
{{#hasFiles}}
<form action="{{PATH}}" method="post">

	{{#files}}
		<input id="{{checkboxId}}" type="checkbox" name="files[]" value="{{path}}" />
		<label for="{{checkboxId}}">{{basename}}</label>
		<br>
	{{/files}}

	<p>
		<button type="button">...</button>
		<button type="submit">→</button>

</form>
{{/hasFiles}}
